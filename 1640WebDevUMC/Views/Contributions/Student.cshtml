@* @model IEnumerable<_1640WebDevUMC.Models.Contribution>

<h2>Contributions</h2>
<a asp-controller="Contributions" asp-action="Student">View Student Contributions</a>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Content</th>
            <th>File</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.Content</td>
                <td>
                    @if (!string.IsNullOrEmpty(item.FilePath))
                    {
                        <a href="@item.FilePath" target="_blank">View File</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
 *@