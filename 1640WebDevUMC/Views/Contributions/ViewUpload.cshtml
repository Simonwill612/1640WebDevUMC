@* @model IEnumerable<_1640WebDevUMC.Models.Contribution>

@{
    ViewData["Title"] = "View Uploads";
}

<h1>View Uploads</h1>

@foreach (var contribution in Model)
{
    <p><strong>Contribution ID:</strong> @contribution.ContributionID</p>
    <p><strong>Title:</strong> @contribution.Title</p>

    @if (contribution.FilePath != null && contribution.FilePath.Any())
    {
        <h2>Uploaded Files:</h2>
        <ul>
            @foreach (var file in contribution.FilePath)
            {
                <p>
                    <a href="@Url.Content(file)">@(System.IO.Path.GetFileName(file))</a>
                </p>
            }
        </ul>
    }
    else
    {
        <p>No files uploaded for this contribution.</p>
    }
}

<div>
    <a asp-action="Index">Back to List</a>
</div>
 *@