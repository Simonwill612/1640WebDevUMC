@model IEnumerable<_1640WebDevUMC.Models.ContributionItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Contribution)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UploadDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.File)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Contribution.ContributionID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UploadDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.File.FileID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image.ImageID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContributionItemID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContributionItemID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContributionItemID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
