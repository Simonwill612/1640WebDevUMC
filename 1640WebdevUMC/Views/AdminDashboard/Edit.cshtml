
@model _1640WebDevUMC.Models.UserViewModel
<h2>Edit User</h2>

<form asp-action="EditUser" method="post">
    <input type="hidden" asp-for="Id" />

    <div>
        <label asp-for="Email"></label>
        <input asp-for="Email" />
        <span asp-validation-for="Email"></span>
    </div>

    <div>
        <label asp-for="EmailConfirmed"></label>
        <input asp-for="EmailConfirmed" />
        <span asp-validation-for="EmailConfirmed"></span>
    </div>

    <div>
        <label for="PasswordHash">New Password</label>
        <input id="PasswordHash" name="PasswordHash" type="password" />
    </div>

    <div>
        <label asp-for="Roles"></label>
        <select asp-for="Roles" asp-items="@(new SelectList(Model.AllRoles))" multiple>
        </select>
        <span asp-validation-for="Roles"></span>
    </div>

    <button type="submit">Save</button>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
